Include("Common")
Include("Common_E")

g_pid_lueur = "PID_リュール"
g_pid_Mauve = "PID_M019_モーヴ"
g_key_area = "モーヴアクティブ_済"
g_map_width = 24
g_map_height = 22

g_dragonVeinConfig = {
    {
        point = {3, 5},
        skillId = "SID_DragonVein_M019_P0",
        triggerX = 3,
        triggerZ = 5,
        effectX = 10,
        effectZ = 18,
        targetPoints = {
            {10, 17},
            {10, 18},
            {10, 19}
        }
    },
    {
        point = {6, 20},
        skillId = "SID_DragonVein_M019_P1",
        triggerX = 6,
        triggerZ = 20,
        effectX = 7,
        effectZ = 11,
        targetPoints = {
            {7, 10},
            {7, 11},
            {7, 12}
        }
    },
    {
        point = {6, 15},
        skillId = "SID_DragonVein_M019_P2",
        triggerX = 6,
        triggerZ = 15,
        effectX = 10,
        effectZ = 3.5,
        targetPoints = {
            {10, 2},
            {10, 3},
            {10, 4},
            {10, 5}
        }
    }
}

dragonVeinEffects = {
        {
            triggerX = 6,
            triggerZ = 7,
            effectX = 10,
            effectZ = 18,
            targetPoints = {
                {10, 17},
                {10, 18},
                {10, 19}
            }
        },
        {
            triggerX = 5,
            triggerZ = 17,
            effectX = 7,
            effectZ = 11,
            targetPoints = {
                {7, 10},
                {7, 11},
                {7, 12}
            }
        },
        {
            triggerX = 5,
            triggerZ = 15,
            effectX = 10,
            effectZ = 3.5,
            targetPoints = {
                {10, 2},
                {10, 3},
                {10, 4},
                {10, 5}
            }
        }
    }
summonVortexEffects = {
            {18, 14, 10.0, 0, 0, 0, 5},
            {19, 14, 10.0, 0, 0, 0, 5},
            {20, 14, 10.0, 0, 0, 0, 5},

            {18, 8, 10.0, 0, 0, 0, 5},
            {19, 8, 10.0, 0, 0, 0, 5},
            {20, 8, 10.0, 0, 0, 0, 5},

            {19, 4, 10.0, 0, 0, 0, 5},
            {20, 4, 10.0, 0, 0, 0, 5},
            {21, 4, 10.0, 0, 0, 0, 5},
            {21, 3, 10.0, 0, 0, 0, 5},

            {13, 3, 10.0, 0, 45, 0, 5},
            {14, 4, 10.0, 0, 45, 0, 5},
            {15, 5, 10.0, 0, 45, 0, 5},

            {21, 14, 10.0, 0, 0, 0, 5},
            {21, 13, 10.0, 0, 0, 0, 5},
            {21, 12, 10.0, 0, 0, 0, 5},
            {21, 11, 10.0, 0, 0, 0, 5},
            {21, 10, 10.0, 0, 0, 0, 5},
            {21, 9, 10.0, 0, 0, 0, 5},
            {21, 8, 10.0, 0, 0, 0, 5},

            {10, 5, 10.0, 0, 0, 0, 5},
            {10, 4, 10.0, 0, 0, 0, 5},
            {10, 3, 10.0, 0, 0, 0, 5},
            {10, 2, 10.0, 0, 0, 0, 5},

            {17, 13, 10.0, 0, 0, 0, 5},
            {17, 12, 10.0, 0, 0, 0, 5},
            {17, 12, 10.0, 0, 0, 0, 5},
            {17, 11, 10.0, 0, 0, 0, 5},
            {17, 10, 10.0, 0, 0, 0, 5},
            {17, 9, 10.0, 0, 0, 0, 5},
            {17, 8, 10.0, 0, 0, 0, 5},
            {17, 7, 10.0, 0, 0, 0, 5},
            {17, 6, 10.0, 0, 0, 0, 5},
            {17, 5, 10.0, 0, 0, 0, 5},

            {14, 16, 10.0, 0, 0, 0, 5},
            {14, 17, 10.0, 0, 0, 0, 5},
            {14, 18, 10.0, 0, 0, 0, 5},
            {13, 18, 10.0, 0, 0, 0, 5},
            {12, 18, 10.0, 0, 0, 0, 5},
            {15, 18, 10.0, 0, 0, 0, 5},
            {16, 18, 10.0, 0, 0, 0, 5},
            {17, 18, 10.0, 0, 0, 0, 5},

            {10, 17, 10.0, 0, 0, 0, 5},
            {10, 18, 10.0, 0, 0, 0, 5},
            {10, 19, 10.0, 0, 0, 0, 5},

            {10, 13, 10.0, 0, 0, 0, 5},
            {10, 12, 10.0, 0, 0, 0, 5},
            {10, 11, 10.0, 0, 0, 0, 5},

            {7, 12, 10.0, 0, 0, 0, 5},
            {7, 11, 10.0, 0, 0, 0, 5},
            {7, 10, 10.0, 0, 0, 0, 5},

            {11, 11, 10.0, 0, 0, 0, 5},
            {12, 11, 10.0, 0, 0, 0, 5},
            {13, 11, 10.0, 0, 0, 0, 5},
            {14, 11, 10.0, 0, 0, 0, 5},
            {15, 11, 10.0, 0, 0, 0, 5},
            {16, 11, 10.0, 0, 0, 0, 5},

            {21, 21, 10.0, 0, 0, 0, 5},
            {20, 21, 10.0, 0, 0, 0, 5},
            {19, 21, 10.0, 0, 0, 0, 5},
            {19, 20, 10.0, 0, 0, 0, 5},

            {6, 13, 10.0, 0, 0, 0, 5},
            {5, 13, 10.0, 0, 0, 0, 5},
            {4, 13, 10.0, 0, 0, 0, 5},
            {3, 13, 10.0, 0, 0, 0, 5},
            {2, 13, 10.0, 0, 0, 0, 5},
            {1, 13, 10.0, 0, 0, 0, 5},
            {6, 13, 5.0, 0, 0, 0, 5},
            {5, 13, 5.0, 0, 0, 0, 5},
            {4, 13, 5.0, 0, 0, 0, 5},
            {3, 13, 5.0, 0, 0, 0, 5},
            {2, 13, 5.0, 0, 0, 0, 5},
            {1, 13, 5.0, 0, 0, 0, 5}
        }
-- -----------------------------------

function Startup()
	
	Log("Startup");
	
	-- 勝利条件設定：敵将撃破
	WinRuleSetDestroyBoss(true)
	WinRuleSetMID( "MID_RULE_M019_WIN" )
	
	フラグ登録()
	イベント登録()
	
end

function Cleanup()
	
	Log("Cleanup");
	
end

function フラグ登録()
	
	VariableEntry(g_key_area, 0)
	
	E_BattleTalk_VariableEntry()
	
end


-- -----------------------------------

function イベント登録()
	EventEntryTurnAfter(青１ターン開始直後, 1, 1, FORCE_PLAYER);
	EventEntryTurn(勝利条件, 1, 1, FORCE_PLAYER)
	EventEntryTurn(RestoreDeletedEffects,1,1,FORCE_ENEMY)

    local dragonVeinPoints = {}
    for _, config in ipairs(g_dragonVeinConfig) do
        table.insert(dragonVeinPoints, config.point)
    end
    DragonVeinRegisterEvents(dragonVeinPoints)
	-- モーヴ
	EventEntryArea(モーヴアクティブ化, 9, 7, 24, 16, FORCE_PLAYER, g_key_area)

	
	-- 村訪問
	EventEntryVisit(村１訪問イベント, 10, 6,"村訪問１_済")
	EventEntryVisit(村２訪問イベント, 15, 20,"村訪問２_済")


	-- ザフィーア会話
	EventEntryTalk(ザフィーア会話, g_pid_lueur,			FORCE_PLAYER, "PID_ザフィーア", FORCE_ALLY, true, "ザフィーア会話_済",	"MID_TK1")
	EventEntryTalk(ザフィーア会話, "PID_ディアマンド",	FORCE_PLAYER, "PID_ザフィーア", FORCE_ALLY, true, "ザフィーア会話_済",	"MID_TK2")
	EventEntryTalk(ザフィーア会話, "PID_スタルーク",	FORCE_PLAYER, "PID_ザフィーア", FORCE_ALLY, true, "ザフィーア会話_済",	"MID_TK3")
	EventEntryDie(EmptyFunction, "PID_ザフィーア", FORCE_ALLY, "S_死亡セリフ_ザフィーア_済")
	
	-- マロン
	E_BattleTalkEntry_Marron( "PID_M019_マロン" )
	EventEntryBattleTalk(Talk, "", FORCE_PLAYER, "PID_M019_マロン", FORCE_ENEMY, true, "戦闘前会話_マロン_済", "MID_BT1");
	EventEntryDie(Talk, "PID_M019_マロン", FORCE_ENEMY, condition_true, "MID_BT3");
	EventEntryReviveAfter(	マロン暴走後, "PID_M019_マロン", FORCE_ENEMY, "マロン暴走後_済" )
	
	-- モーヴ
	EventEntryBattleTalk(Talk, "", FORCE_PLAYER, "PID_M019_モーヴ", FORCE_ENEMY, true, "戦闘前会話_モーヴ_済", "MID_BT4");
	EventEntryDie(Talk, "PID_M019_モーヴ", FORCE_ENEMY, condition_true, "MID_BT6");
	EventEntryReviveAfter(	モーヴ暴走後, "PID_M019_モーヴ", FORCE_ENEMY, "モーヴ暴走後_済" )
	
	-- --------------------------------------------------
	EventEntryEscape(離脱イベント, 2, 10, "PID_リュール", condition_true)
    EventEntryEscape(離脱イベント, 24, 11, "", condition_true)
end

-- -----------------------------------

function Opening()
	
	Log("Opening");
    --
	--PuppetDemo("M019", "MID_OP1")
	--PuppetDemo("M019", "MID_OP2")

end

function MapOpening()
	initMapM019()
	Log("MapOpening");
	CursorSetPos_FromPid( "PID_M019_マロン" )
	Talk("MID_OP3")
	UnitMovePos("PID_M019_マロン",	24 ,11, MOVE_FLAG_NONE)
	UnitMovePos("PID_M019_モーヴ",	19 ,11, MOVE_FLAG_NONE)
	UnitMoveWait()
	UnitRotation( "PID_M019_マロン", ROTATE_DOWN_LEFT )
	UnitMoveWait()
	--瘴気配置()
end

function initMapM019()

    ClearAllMiasma()
    local magicCirclePoints = {}
    for _, config in ipairs(g_dragonVeinConfig) do
        table.insert(magicCirclePoints, config.point)
    end
    DragonVeinCreateMagicCircle(magicCirclePoints)

    local terrainPoints = {}
    for _, config in ipairs(g_dragonVeinConfig) do
        table.insert(terrainPoints, {config.point[1], config.point[2], "TID_龙脉_M019_P"})
    end
    DragonVeinSetTerrain(terrainPoints)

    TerrainSetBegin()
    for i, effect in ipairs(summonVortexEffects) do
        EffectCreateRotateXYZ("瘴気", effect[1], effect[2], effect[3], effect[4], effect[5], effect[6], effect[7])
        local currentTerrain = TerrainGet(effect[1],effect[2])
        local newTerrain = currentTerrain .. "_M019"
        TerrainSet(effect[1],effect[2],newTerrain)

    end
    TerrainSetEnd()

end

function DragonVeinUsed()
    local unit = MindGetUnit()
    local x = UnitGetX(unit)
    local z = UnitGetZ(unit)

    local dragonVeinPoints = {}
    for _, config in ipairs(g_dragonVeinConfig) do
        table.insert(dragonVeinPoints, {config.point, config.skillId, "bomb", 0})
    end

    local veinInfo = DragonVeinPreUseCheckWithTid(dragonVeinPoints,"TID_DragonVein_Used", x, z, 7, 11, g_map_height, g_map_width)
    if veinInfo then
        DragonVeinPostUseHandleM019(veinInfo)
    end
end

function DragonVeinPostUseHandleM019(veinInfo)
    TerrainSetBegin()
        TerrainSet(veinInfo.x, veinInfo.z, "TID_床")
    EffectDelete("魔法陣_G001", veinInfo.x, veinInfo.z)
    CursorSetPos(veinInfo.x, veinInfo.z)
    WaitTime(1.0)
    EffectPlay("水球_発動", veinInfo.x, veinInfo.z)
    EffectWait()

    for _, effectConfig in ipairs(dragonVeinEffects) do
        if veinInfo.x == effectConfig.triggerX and veinInfo.z == effectConfig.triggerZ then
            CursorSetPos(effectConfig.effectX, effectConfig.effectZ)
            WaitTime(1.0)
            EffectPlay("カムイ竜脈", effectConfig.effectX, effectConfig.effectZ)
            EffectWait()
            for _, point in ipairs(effectConfig.targetPoints) do
                TerrainSet(point[1], point[2], "TID_床")
                EffectDelete("瘴気", point[1], point[2])
            end

            break
        end
    end

    TerrainSetEnd()
end

function RestoreDeletedEffects()
    local x = UnitGetX(g_pid_Mauve)
    local z = UnitGetZ(g_pid_Mauve)
    CursorSetPos(x,z)
    WaitTime(1.0)
    EffectPlay( "邪竜紋_発動", x, z )
    WaitTime( 1.5 )
    EffectWait()

    CursorSetPos(g_map_width/2, g_map_height/2)
    WaitTime(1.0)
    EffectPlay("カムイ竜脈", g_map_width/2, g_map_height/2)
    EffectWait()

    TerrainSetBegin()
    for _, effect in ipairs(summonVortexEffects) do
        local x = effect[1]
        local z = effect[2]

        local currentTerrain = TerrainGet(x, z)

        if string.sub(currentTerrain, -5) ~= "_M019" then
            EffectCreateRotateXYZ("瘴気", x, z, effect[3], effect[4], effect[5], effect[6], effect[7])

            local newTerrain = currentTerrain .. "_M019"
            TerrainSet(x, z, newTerrain)
        end
    end
    TerrainSetEnd()
    rebuildDragonVeinInfo()
    ProcessAllUnitsLanding(g_map_width,g_map_height)
end

function rebuildDragonVeinInfo()
    local skillIds = {}
    local dragonVeinPoints = {}
    local magicCirclePoints = {}
    local terrainPoints = {}

    for _, config in ipairs(g_dragonVeinConfig) do
        local x, z = config.point[1], config.point[2]
        if TerrainGet(x, z) ~= "TID_龙脉_M019_P" then
            table.insert(skillIds, config.skillId)
            table.insert(dragonVeinPoints, config.point)
            table.insert(magicCirclePoints, config.point)
            table.insert(terrainPoints, {x, z, "TID_龙脉_M019_P"})
        end
    end

    DragonVeinEquipSkills(skillIds)
    DragonVeinRegisterEvents(dragonVeinPoints)
    DragonVeinCreateMagicCircle(magicCirclePoints)
    DragonVeinSetTerrain(terrainPoints)
end


function ClearAllMiasma()
	TerrainSetBegin()
	for i = 1, g_map_width do
		for j = 1, g_map_height do
			if ( MapOverlapGet( i, j ) == "TID_瘴気_永続") then

				MapOverlapSet( i , j, "TID_無し" )
			end
		end
	end
	MapOverlapSetEnd()
end
-- -----------------------------------

function EmptyFunction()
end

-- -----------------------------------

function 青１ターン開始直後()

	CursorAnimeCreate(2,11)
	Talk( "MID_EV4" )
	CursorAnimeDelete()

	CursorAnimeCreate_FromPid("PID_ザフィーア")
	Talk( "MID_EV1" )
	CursorAnimeDelete()

	local skillIds = {}
    for _, config in ipairs(g_dragonVeinConfig) do
        table.insert(skillIds, config.skillId)
    end
    DragonVeinEquipSkills(skillIds)
end

function ザフィーア会話( mid )
	Talk(mid)
	
	-- 緑軍→青軍
	pid = "PID_ザフィーア"
	if UnitExistOnMap( pid ) and ( UnitGetForce(pid) == FORCE_ALLY ) then
		UnitJoin( pid );
	end
end

-- -----------------------------------
function 増援３()
	
	Dispos( "Enemy_Reinforcement3", DISPOS_FLAG_FOCUS )
	Yield()
	WaitTime(0.5)
	
end
function 増援４()
	
	Dispos( "Enemy_Reinforcement4", DISPOS_FLAG_FOCUS )
	Yield()
	WaitTime(0.5)
	
end
function 増援５()
	
	Dispos( "Enemy_Reinforcement5", DISPOS_FLAG_FOCUS )
	Yield()
	WaitTime(0.5)
	
end

function モーヴアクティブ化()

	--	ここで何某か会話イベントして、エンゲージ技の設定とかする。
	--AiSetSequence("PID_オルテンシア", AI_ORDER_ATTACK, "AI_AT_EngageAttack")



	AiSetSequence("PID_M019_モーヴ", AI_ORDER_CAUSE, "AI_AC_Everytime")	--	AI_AC_InterferenceRange
	AiSetSequence("PID_M019_モーヴ", AI_ORDER_ATTACK, "AI_AT_RodRescue","1,1")
--	AiSetSequence("PID_M019_モーヴ", AI_ORDER_ATTACK, "AI_AT_Attack")	--	AI_AC_InterferenceRange
	UnitClearStatus("PID_M019_モーヴ", UNIT_STATUS_MOVE_NOT_ALLOW)

	VariableSet(g_key_area, 1)


	--Dispos( "Enemy_Reinforcement4", DISPOS_FLAG_FOCUS )
	--Yield()
	--WaitTime(0.5)

end

-- -----------------------------------

function モーヴ暴走後()
	CursorSetPos_FromPid( "PID_M019_モーヴ" )
	Yield()
	WaitTime(0.5)

--	Dialog("モーヴ暴走")
	Talk("MID_BT8");
	--Talk( "MID_EV1" )	--暴走
	UnitClearStatus("PID_M019_モーヴ", UNIT_STATUS_MOVE_NOT_ALLOW)
--	UnitSetItemEquip( "PID_M019_モーヴ", "IID_ミカヤ_セイニー" )
	--ItemGainSilent(string pid, string iid)
--	AiSetPriority("PID_モーヴ", 50)


--	AiSetPriority("PID_M014_マロン", 100)

	AiSetSequence("PID_M019_モーヴ", AI_ORDER_ATTACK, "AI_AT_RodRescue","1,1")

--	AiSetSequence("PID_M014_イルシオン兵_ブレイブヒーロー", AI_ORDER_CAUSE, "AI_AC_Everytime")
end

function マロン暴走後()
	CursorSetPos_FromPid( "PID_M019_マロン" )
	Yield()
	WaitTime(0.5)


--	Dialog("マロン暴走")
	Talk("MID_BT7");

	AiSetSequence( "PID_M019_マロン", AI_ORDER_ATTACK, "AI_AT_EngageAttack", "1, 1" )

	--Talk( "MID_EV1" )	--暴走
	UnitSetItemEquip( "PID_M019_マロン", "IID_ロイ_封印の剣" )
	--ItemGainSilent(string pid, string iid)
--	AiSetPriority("PID_モーヴ", 50)
	AiSetSequence("PID_M019_モーヴ", AI_ORDER_CAUSE, "AI_AC_Everytime")	--	AI_AC_InterferenceRange
	AiSetSequence("PID_M019_モーヴ", AI_ORDER_ATTACK, "AI_AT_RodRescue","1,1")
	VariableSet(g_key_area, 1)


--	AiSetPriority("PID_M014_マロン", 100)


--	AiSetSequence("PID_M014_イルシオン兵_ブレイブヒーロー", AI_ORDER_CAUSE, "AI_AC_Everytime")
end

-- ---------------------------------------------------------

function 村１訪問イベント()
	Talk("MID_EV2");

	Dispos( "Enemy_Reinforcement1", DISPOS_FLAG_FORCED )
	Yield()
	WaitTime(0.5)

end
function 村２訪問イベント()
	Talk("MID_EV3");

	Dispos( "Enemy_Reinforcement2", DISPOS_FLAG_FORCED )
	Yield()
	WaitTime(0.5)

end


-- -----------------------------------

function 瘴気配置()

	MapOverlapSetBegin()
	for i = 8 , 23 do
		for j = 1 , 22 do
			MapOverlapSet(i, j, "TID_瘴気_永続")
		end
	end
	MapOverlapSetEnd()
end

-- -----------------------------------

function 離脱イベント()
	local unit = MindGetUnit()
	if not UnitExistOnMap( unit ) then
		return
	end
	
	UnitRotation( g_pid_lueur, ROTATE_UP)
	UnitMoveWait()
	
	VariableSet( "勝利", 1 )
end

-- -----------------------------------



function MapEnding()
	
	Log("MapEnding");
	
	
end

function Ending()
	
	Log("Ending");
	
	-- 紋章士ロイと紋章士ミカヤの指輪が返ってきた
	GodUnitSetEscape("GID_ロイ",	false)
	GodUnitSetEscape("GID_ミカヤ",	false)
	
	PuppetDemo("M019", "MID_ED1")
	PuppetDemo("M019", "MID_ED2")
	
end

function GameOver()
	
	Log("GameOver");
	
end
